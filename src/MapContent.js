import React from 'react';

const MapComponent = ({ selectedProfile }) => {
  // Use a mapping library like Google Maps or Mapbox here
  // Show the selectedProfile's address with a marker
  return <div className="map">Map goes here</div>;
};

export default MapComponent;
